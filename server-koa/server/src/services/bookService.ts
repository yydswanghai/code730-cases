import Book, { BookAttributes } from '../models/Book'
import { validators, async } from 'validate.js'

// type test = {
//     [p in 'name' | 'imgurl']: BookAttributes[p]
// }

// 添加
export async function addBook(obj) {
    
}